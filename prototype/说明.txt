原型模式

本质：
	克隆生成对象

优点：
1	对客户端隐藏具体的实现类型
2	在运行时动态改变具体的实现类型
缺点：
1	原型的子类都必须实现clone的操作，尤其在包含引用类型的对象时，clone方法会比
	较麻烦，必须要能够递归地让所有的相关对象都要正确地克隆。

使用情况：
1	如果一个系统想要独立于它想要使用的对象是，可以使用原型模式，让系统只面向接
	口编程，在系统需要新的对象的时候，可以通过克隆原型来得到。
2	如果需要实例化的类是在运行时刻动态指定时，可以使用原型模式，通过克隆原型来
	得到需要的实例。